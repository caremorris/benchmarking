Benchmarking databases: SQLite, Influx, Tempo, Xively

For each database, perform and time the following operations:
*insert points
*select first point
*select last point
*select a range of points
Export results for each database as a CSV file.
